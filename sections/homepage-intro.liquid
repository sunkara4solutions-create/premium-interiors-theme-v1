<section class="intro" id="about">
  <div class="intro-visual">
    {% if section.settings.intro_image %}
      <img src="{{ section.settings.intro_image | img_url: '800x' }}" alt="Wood Panel" class="intro-real-image" />
    {% else %}
      <div class="intro-panel-mock">
        <div class="intro-slats">
          {% for i in (1..16) %}<div class="slat"></div>{% endfor %}
        </div>
        <div class="intro-glow-overlay"></div>
      </div>
    {% endif %}
    <div class="intro-badge">
      <strong>25%</strong>
      OFF Sale
    </div>
  </div>

  <div class="intro-text">
    <div class="section-label">{{ section.settings.label }}</div>
    <h2 class="section-title">{{ section.settings.heading }}</h2>
    <div class="intro-body">{{ section.settings.body | newline_to_br }}</div>
    <div class="intro-stats">
      <div>
        <div class="stat-num">25%</div>
        <div class="stat-label">Noise Reduction</div>
      </div>
      <div>
        <div class="stat-num">{{ collections.all.products_count }}</div>
        <div class="stat-label">Premium Products</div>
      </div>
      <div>
        <div class="stat-num">30<span style="font-size:1rem">min</span></div>
        <div class="stat-label">Avg. Install Time</div>
      </div>
      <div>
        <div class="stat-num">5★</div>
        <div class="stat-label">Customer Rating</div>
      </div>
    </div>

    <!-- FINISHES SECTION -->
    <div class="finishes-block">
      <div class="finishes-desc">{{ section.settings.finishes_desc }}</div>
      <div class="finish-swatches">
        <div class="finish-item">
          <div class="finish-swatch" style="background: linear-gradient(135deg, #0d0d0d 0%, #1c1c1c 50%, #0d0d0d 100%);"></div>
          <span class="finish-name">Black Oak</span>
        </div>
        <div class="finish-item">
          <div class="finish-swatch" style="background: linear-gradient(135deg, #8b5e3c 0%, #c49a6a 50%, #8b5e3c 100%);"></div>
          <span class="finish-name">Natural Walnut</span>
        </div>
        <div class="finish-item">
          <div class="finish-swatch" style="background: linear-gradient(135deg, #c4913a 0%, #dab060 50%, #c4913a 100%);"></div>
          <span class="finish-name">Warm Oak</span>
        </div>
        <div class="finish-item">
          <div class="finish-swatch" style="background: linear-gradient(135deg, #3d3028 0%, #5a4838 50%, #3d3028 100%);"></div>
          <span class="finish-name">Charcoal</span>
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Homepage Intro",
  "settings": [
    { "type": "image_picker", "id": "intro_image", "label": "Panel Image (optional)" },
    { "type": "text", "id": "label", "label": "Label", "default": "About Our Panels" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Crafted for the Discerning Eye" },
    { "type": "textarea", "id": "body", "label": "Body Text" },
    { "type": "textarea", "id": "finishes_desc", "label": "Finishes Description", "default": "Available in Black Oak, Natural Walnut, Warm Oak, and Charcoal finishes — designed to complement modern, industrial, and transitional interiors alike." }
  ],
  "presets": [{ "name": "Homepage Intro" }]
}
{% endschema %}
